
const initial = {
  loggedIn: false,

};

const commonReducer = (state = initial, action: { type: string }) => {
  switch (action.type) {

    default:
      return state;
  }
};
export default commonReducer;
